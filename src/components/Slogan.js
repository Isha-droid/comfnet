const Slogan=() =>
{
        return (
                <div className="bg-blue-700 py-12 text-center text-xl text-bold">‘We don't just connect regions; we empower businesses to <br></br>thrive on the global stage’</div>
        )
}

export default Slogan